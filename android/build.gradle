buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.2'  // Ensure this is up to date
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.5.31'  // Ensure Kotlin version is correct
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
